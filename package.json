{
  "name": "admin.lesichkov.co.uk",
  "version": "1.0.0",
  "scripts": {
    "serve": "browser-sync start --server 'docs' --files '*.html, css/*.css'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "proprietary",
  "devDependencies": {
    "browser-sync": "^2.26.7"
  }
}
